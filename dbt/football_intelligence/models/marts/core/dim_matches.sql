select
  cast(null as string) as match_id,
  cast(null as string) as competition_id,
  cast(null as string) as season_id,
  cast(null as date) as match_date,
  cast(null as string) as home_team_id,
  cast(null as string) as away_team_id
where false
